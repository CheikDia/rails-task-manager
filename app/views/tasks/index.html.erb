<h1>My Tasks</h1>
<ol>
  <% @tasks.each do |task| %>
    <% if task.completed %>
      <li> [X] <%= link_to task.title, task_path(task) %></li>
    <% else %>
      <li> [ ] <%= link_to task.title, task_path(task) %></li>
    <%end%>
  <% end %>
</ol>

